import { Typography } from '@mui/material';
import React from 'react';

function NotFoundInDashboard() {
    return (
        <div>
            <Typography variant="h4">This page is not accessible in the dashboard !</Typography>
        </div>
    );
}

export default NotFoundInDashboard;
